__pycache__/
*.pyc
.DS_Store

# Google service-account credentials + Sheet ID. NEVER commit this —
# treat it like a password. Lives only in .streamlit/secrets.toml
# locally and in the Streamlit Cloud "Secrets" panel for the deployed
# app. See SETUP.md.
.streamlit/secrets.toml

# Legacy local-disk store, no longer the source of truth now that
# Google Sheets is (see services/sheets_store.py). Kept only as an
# emergency fallback if Sheets is unreachable — see
# services/data_source.py::_local_fallback_snapshot(). Still not
# source-controlled data even in that fallback role.
session_data.json
venv/
